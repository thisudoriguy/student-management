/*const mongooose = require('mongoose');

const classSchema = new mongooose.Schema({
    student:{
        type: Schema.ObjectId, ref: Student, required
    },
    Books,
    Subjects,
    ClassTeacher,
    SubjectTeachers
    
})

module.exports = mongoose.model('Class', classSchema)
*/